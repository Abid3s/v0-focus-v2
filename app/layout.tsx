import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { ThemeProvider } from '@/components/theme-provider'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Focus Insurance Services | UK Insurance Brokers | FCA Regulated',
  description: 'Focus Insurance Services - Leading UK insurance brokers offering personal, commercial, and specialist insurance solutions. FCA regulated with competitive rates and expert advice.',
  keywords: 'insurance broker, UK insurance, personal insurance, commercial insurance, specialist insurance, FCA regulated, competitive rates',
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem={false}
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
